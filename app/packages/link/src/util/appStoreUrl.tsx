// import { detectBrowser } from "./detectBrowser";

// eslint-disable-next-line @typescript-eslint/no-unused-vars
export function appStoreUrl(agent?: string) {
  // const browser = detectBrowser(agent)

  // if (browser === 'android') return 'https://play.google.com/store/apps/details?id=com.polybaselabs.wallet'

  return 'https://testflight.apple.com/join/rKCavMy1'
}
