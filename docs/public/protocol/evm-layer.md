# EVM Layer

The EVM Layer is a Ethereum compatible interface for Payy that adheres to the Ethereum transaction model and EVM byte code. All transactions occur through the EVM Layer, including transactions which will only be processed on the Privacy Layer (i.e. for private transfers).

See [Ethereum Compatibility](../build-on-payy/ethereum-compatibility.md).
